鼓浪屿项目 Unity 场景准备傻瓜教程

适用范围
1. 本文档只处理 Unity 场景准备。
2. 先不写新代码，不处理后端，不处理前端。
3. 目标是把 Unity 场景先整理好，等你准备完成后，我再继续接后面的代码。

你现在要达到的结果
1. 明确你后面要使用的 Unity 工程。
2. 场景里有可测试的 A 组、B 组 Agent。
3. 场景里有清晰的起点、终点。
4. 场景里有准备好的危险区域位置。
5. 场景里没有明显的基础错误，比如多个 AudioListener。
6. 后面一旦我开始写代码，你只需要把脚本挂上去就能测试。

第一部分：先确认你到底用哪个 Unity 工程

第1步
打开你现在真正打算继续开发和测试的 Unity 工程。

第2步
不要两个 Unity 工程来回切。

第3步
如果你后面准备继续在当前这个 3D 场景里做实验，就固定使用这一个工程，不要中途换。

第4步
在开始前，先保存场景。
操作：
File -> Save

第5步
建议你另存一个测试场景副本，防止后面改乱。
操作：
File -> Save As
建议名字：
SampleScene_ABTest

第二部分：整理场景层级

第1步
在 Hierarchy 里找到或新建一个总管理空物体。
名字建议：
ABTestRoot

第2步
如果没有这个物体，就创建：
Hierarchy 空白处右键 -> Create Empty

第3步
把下面这些测试专用物体都放到 ABTestRoot 下面，方便管理：
1. Spawn_A
2. Spawn_B
3. Target_A
4. Target_B
5. 后面要做危险区的物体

第4步
如果你已经有这些物体，只要把它们拖进 ABTestRoot 下面即可。

第三部分：准备 A 组和 B 组 Agent

第1步
确认你已经有两个 Agent 预制体，或者至少有一个可用的 Agent 预制体。

推荐最终状态
1. Agent_A.prefab
2. Agent_B.prefab

第2步
如果现在只有一个 AgentPrefab，就复制两份。
操作：
Project 面板里选中现有 prefab
Ctrl + D 复制两次
分别重命名为：
Agent_A
Agent_B

第3步
给两个预制体做颜色区分。
推荐：
1. A 组红色
2. B 组蓝色

第4步
进入 Prefab 查看每个 Agent 至少要有这些基础组件：
1. Collider
2. Rigidbody
3. NavMeshAgent
4. DigitalCane

第5步
检查 Rigidbody 设置。
建议：
1. 勾选 Freeze Rotation X
2. 勾选 Freeze Rotation Y
3. 勾选 Freeze Rotation Z

第6步
检查 NavMeshAgent 是否存在。
如果没有：
Add Component -> NavMeshAgent

第7步
检查 Agent 上有没有 AudioListener。
如果有多个 Agent 都带 AudioListener，请全部删掉，只保留 Main Camera 上的一个 AudioListener。

你之前已经出现过多个 AudioListener 提示，所以这一步一定要做。

第四部分：清理摄像机和音频

第1步
在 Hierarchy 里点 Main Camera。

第2步
看 Inspector，确认 Main Camera 上保留 AudioListener。

第3步
再分别点开 Player、Agent_A、Agent_B。

第4步
如果它们身上也有 AudioListener：
右键组件标题 -> Remove Component

第5步
最终目标：
整个场景里只保留一个 AudioListener。

第五部分：放置 A 组和 B 组起点终点

第1步
在 ABTestRoot 下确认这四个空物体存在：
1. Spawn_A
2. Spawn_B
3. Target_A
4. Target_B

第2步
如果没有，就创建。
操作：
Hierarchy 右键 -> Create Empty

第3步
把 Spawn_A 放到 A 组出发点。

第4步
把 Spawn_B 放到 B 组出发点。

第5步
把 Target_A 放到 A 组目标点。

第6步
把 Target_B 放到 B 组目标点。

第7步
建议当前阶段先把距离拉远一点，不要太近。
推荐：
1. 让起点和终点之间至少能走出一段完整路线
2. 最好经过楼梯、坡道、边缘或狭窄区域
3. 不要近到两秒钟就走完

第8步
如果你只是先做对照测试，建议：
1. A 组和 B 组终点可以先放在同一片区域附近
2. 但两个起点不要完全重叠
3. 避免一出生就互相顶住

第9步
把四个空物体的位置放好后，切换到顶视图和透视图都看一遍，确认没有放到墙里、楼里或 NavMesh 外面。

第六部分：确认 NavMesh 可走区域

第1步
打开导航显示。

如果你当前版本是 AI Navigation 组件方式，就在场景里查看已经烘焙好的 NavMesh 可走颜色区域。

第2步
检查 Spawn_A 和 Spawn_B 是否都落在可走区域上。

第3步
检查 Target_A 和 Target_B 是否都落在可走区域上。

第4步
如果不在可走区域上，稍微挪一下位置，不要强行放在不可行走的屋顶、墙体、模型内部。

第5步
如果场景改动后可走区域明显不对，需要重新烘焙 NavMesh。

第七部分：先把危险区域物体找出来

这一部分先做场景准备，不挂新脚本。

第1步
在场景中找出你后面想统计风险的区域。

建议至少选 3 类：
1. 楼梯
2. 坡道
3. 狭窄障碍区或边缘区

第2步
给这些区域做一个统一整理，方便后面挂脚本。

推荐做法：
1. 新建一个父物体叫 HazardAreas
2. 把相关危险区域物体都放进去

第3步
如果原模型本身不好直接挂组件，就新建简单辅助物体覆盖危险范围。

推荐：
1. 用 Cube 或 Empty + Box Collider
2. 位置覆盖住危险区域
3. 命名清楚

例如：
1. Hazard_Stairs_01
2. Hazard_Slope_01
3. Hazard_Obstacle_01

第4步
先不急着加脚本，但要把范围框好。

第5步
后面等我开始写代码时，你只需要给这些物体加 Collider 和挂脚本。

第八部分：先把 Collider 准备思路想好

第1步
后面危险区域建议使用 Trigger Collider，而不是靠模型本体碰撞乱触发。

第2步
所以你现在先检查这些危险区域物体能不能方便地加 Box Collider。

第3步
原则是：
1. Collider 不要过大
2. 刚好覆盖危险区
3. 不要把整条路都包住

第4步
如果是楼梯，Collider 覆盖主要踩踏区域即可。

第5步
如果是坡道，Collider 覆盖坡面核心部分即可。

第6步
如果是边缘或障碍区，Collider 覆盖最容易触发危险的位置。

第九部分：为后面的算法路线预留场景标记

这一部分也先做准备，不写代码。

第1步
如果你后面要做真正的“算法组 B”，建议现在就把场景里的关键路径点整理出来。

第2步
做法：
在场景里新建一个父物体，名字建议：
RouteMarkers

第3步
在 RouteMarkers 下面，逐个放置一些 Empty 作为未来路线标记点。

第4步
这些点先放在你认为 B 组安全路径可能经过的位置，例如：
1. 路口
2. 转角
3. 楼梯前
4. 坡道入口
5. 目标建筑前

第5步
现在不用一次放很多，先放 5 到 10 个关键点就够。

第6步
命名时尽量统一，不要一会中文一会英文，一会带空格一会不带空格。

第十部分：检查 Agent 是否真的能跑

第1步
使用你当前已经能跑的 A/B 测试方式，点一次 Play。

第2步
观察以下几点：
1. A 组是否正常生成
2. B 组是否正常生成
3. 会不会一出生就卡住
4. 会不会掉出可走区域
5. 会不会直接撞墙不动

第3步
如果有卡住，就先改 Spawn_A 和 Spawn_B 的位置。

第4步
如果有目标点到不了，就先改 Target_A 和 Target_B 的位置。

第5步
这一轮只看基础移动是否流畅，不管算法，不管后端。

第十一部分：你改完 Unity 之后要达到的检查清单

请你逐条自查：

1. 我已经确定只用一个 Unity 工程继续做。
2. 我已经保存了测试场景副本。
3. 场景里有 ABTestRoot。
4. 场景里有 Spawn_A、Spawn_B、Target_A、Target_B。
5. Spawn 和 Target 都在可走区域上。
6. 我已经准备好 Agent_A 和 Agent_B 预制体。
7. Agent 上需要的基础组件都齐了。
8. 整个场景只剩一个 AudioListener。
9. 我已经整理好危险区域位置。
10. 我已经准备好后面要挂 Trigger 的危险区物体。
11. 我已经预留了一些未来算法路线点。
12. 当前 A/B 组再次 Play 仍然能跑。

第十二部分：你做完后怎么告诉我

当你做完上面这些，请直接告诉我下面这句话：

“Unity 场景准备完成，可以开始写 HazardZone、RouteFollower 和 ABTestController 改造了。”

如果你还没完全做完，也可以这样告诉我：

“我做到了第几部分，第几个步骤，卡在什么地方。”

结束语

你现在先不要急着继续写代码。
先把 Unity 场景整理干净，后面我再继续帮你做下一步。
